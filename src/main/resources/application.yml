spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: test-mongo
      uri: mongodb://localhost/test-mongo
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG